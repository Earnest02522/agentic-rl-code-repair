# MCP Tool Environment

Future extension for real MCP-backed tools.

Do not implement this before the `code_exec` environment is working. MCP support should reuse the same action, observation, trajectory, verifier, and attribution schema.

