# Database Environment

Future extension for SQL/database tool tasks.

Potential tasks:

- query a database
- diagnose wrong SQL
- repair queries after execution errors
- compare query outputs against expected answers

Do not prioritize this until the D2C / Code Agent loop is stable.

