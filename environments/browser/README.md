# Browser Environment

Future extension for browser-based interaction and visual verification.

Potential tools:

- open page
- click
- type
- inspect DOM
- take screenshot
- run accessibility checks

Keep this behind the same trajectory schema used by the code execution environment.

